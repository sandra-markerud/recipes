query FetchRecipeById(
    $id: ID!
) {
    recipe(id: $id) {
        id
        name
        instruction
        ingredients {
            id
            quantity
            unit {
                longName
            }
            food {
                id
                name
            }
        }
    }
}

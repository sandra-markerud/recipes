mutation CreateUnit($longName: String!, $shortName: String!) {
    createUnit(input: { longName: $longName, shortName: $shortName }) {
        unit {
            id
            longName
            shortName
        }
    }
}
